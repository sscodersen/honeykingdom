query SphrexStats {
  globalProtocolStats(request: { sources: "Sphrex" }) {
    totalProfiles
    totalPosts
    totalBurntProfiles
    totalMirrors
    totalComments
    totalCollects
    totalFollows
  }
}
